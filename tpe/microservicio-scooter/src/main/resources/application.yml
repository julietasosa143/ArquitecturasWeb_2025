spring:
    data:
      mongodb:
        uri: mongodb://mongouser:mongopassword@localhost:27017/db_scooter?authSource=admin

server:
  port: 8083 # Puerto diferente si el microservicio de Usuario est� en 8081
